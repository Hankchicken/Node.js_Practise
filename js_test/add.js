const person = { name: 'Hank', age: 18 };
const { name, age } = person;
console.log(`姓名: ${name}, 年齡: ${age} `);

const person1 = { name1: 'Hank', age1: '25' }
const { name1, age1 } = person1;
console.log('身分', name, age);